import styled from "styled-components";

const FooterSection = styled.footer`
    display: flex;
`

const Footer = () => {
    return (
        <FooterSection>

        </FooterSection>
    )
}

export default Footer;